@{
    ViewData["Title"] = "AdminPanel";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

<div class="text-center d-flex flex-column align-items-center gap-4 mt-5">
    <h1 class="display-4">Добро пожаловать!</h1>
    <div class="d-flex flex-row gap-3">
        <a href="@Url.Action("Index", "Road")" class="btn btn-warning btn-lg">Дороги</a>
        <a href="@Url.Action("Index", "Spot")" class="btn btn-warning btn-lg">Места</a>
    </div>
</div>